{{#each eachSem}}
    <div class="card mt-2">
        <div class="card-body">
            <h3> <a href="/semesters/show_marks/semester_number={{sem_number}}/{{id}}">SEM NO: {{sem_number}}</a></h3>
            <p>College: {{college_name}}</p>
            <a href="/add_marks/{{id}}"><button class="btn btn-primary">Add Marks</button></a>
            <a href="/edit_marks/semester_number={{sem_number}}/{{id}}"><button class="btn btn-default">Edit Marks</button></a>
        </div>
    </div>
{{/each}}